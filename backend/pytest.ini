[pytest]
markers =
    unit:   Pure unit tests — no real images needed. Fast, always runnable.
    visual: Tests requiring real eye photos in tests/test_images/. Skip if no images.
asyncio_mode = auto
